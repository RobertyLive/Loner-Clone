verificato un problema durante l'accesso al servizio Windows Installer.\r\n\r\n**Correzione**> È consigliabile riavviare il sistema._Esporta_Esporta configurazionezPer garantire la coerenza di carichi di lavoro e componenti, è possibile esportare una configurazione dell'installazione.,Esporta la configurazione dell'installazioneEsportazione — {0}%Esportazione in corso — {0} — {1}Percorso di _esportazionebSi è verificato un problema durante l'esportazione del file di configurazione dell'installazione.Non è possibile esportare^ID del canale che contiene il prodotto per cui esportare la configurazione dell'installazione.JPercorso per il salvataggio del file di configurazione dell'installazione.hPercorso dell'installazione da usare per l'esportazione di un file di configurazione dell'installazione.&Architettura del prodotto da esportareGID del prodotto per cui esportare la configurazione dell'installazione.^Il motivo può essere che le estensioni non sono presenti in [Visual Studio Marketplace]({0}).pLa configurazione di installazione è stata esportata, ma non è stato possibile esportare i pacchetti seguenti:8La configurazione dell'installazione è stata esportata.5Esporta un file di configurazione dell'installazione.>L'estensione ha dipendenze su altre estensioni non installate.TIl caricamento di questa estensione non è riuscito o la versione non è supportata.
Estensione?Il tipo di estensione non è supportato o non è stato caricato�Le seguenti estensioni hanno una firma non valida e potrebbero potenzialmente danneggiare il tuo PC. Richiediamo di deselezionarle prima di continuare.4È stata rilevata una firma di estensione non valida
Estensioni�Per fornire ulteriori dettagli sulle estensioni selezionate, il programma di installazione di Visual Studio deve prima scaricarle.\r\n\r\nAttendi fino al completamento del caricamento.;Le estensioni non sono ancora state completamente scaricate�Per inviare feedback, è necessario avere .NET Standard 4.6.2 o versione successiva. Eseguire l'aggiornamento a .NET 4.6.2 o versione successiva e riprovare.mSe non è possibile aggiornare la versione di .NET, selezionare "Report senza log" per segnalare il problema.�NOTA: si tratta di un'esperienza di creazione di report degradata che non includerà i log che rendono il ticket utilizzabile. Per un'esperienza ottimale, eseguire l'aggiornamento a .NET 4.6.2 o versione successiva.Report senza logIl file {0} non esiste.Non è possibile aprire il fileErrore Come si risolve questo problema?�I framework, gli SDK e gli strumenti necessari per il lavoro di sviluppo selezionato sono già installati. Per aggiungerne altri in qualsiasi momento, eseguire 'Programma di installazione di Visual Studio'.�Verranno installati i framework, gli SDK e gli strumenti necessari. Pe